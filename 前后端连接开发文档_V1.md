# 项目清单筛选查询插件 - 前后端连接开发文档

版本：V1.0  
日期：2026-05-16  
范围：前端、后端 API、Excel 导入/导出、联调验收  
依据：`开发文档_项目清单筛选查询插件_V2.docx`、当前 `frontend/` 与 `backend/` 代码实现

## 1. 文档目标

本阶段目标不是重新设计系统，而是把已经完成的前端页面、后端接口、数据库与 Excel 导入导出能力连接成可运行闭环：

```text
登录 -> 上传/导入 Excel -> 查询筛选 -> 结果展示 -> 查看详情 -> 复核 -> 导出 -> 查询历史/审计留痕
```

原 V2 文档中已经明确系统定位、数据结构、无效数据识别、金额双字段、权限、审计等设计。本文件在此基础上补齐前后端连接时必须遵守的接口契约、启动方式、联调步骤和验收口径。

## 2. 当前系统状态

| 模块 | 当前状态 | 联调结论 |
|---|---|---|
| 前端 | Vue 3 + Vite + TypeScript + Element Plus 已搭建；登录、查询、详情、复核、导入、审计、查询历史页面已具备 | 可通过 mock 独立预览，也可切换真实 API |
| 后端 | FastAPI + SQLAlchemy + SQLite 已搭建；认证、项目查询、导出、导入、查询历史、审计接口已具备 | 可作为真实联调服务 |
| 数据层 | `projects`、`users`、`audit_logs`、`query_history` 表结构已具备 | 支持当前业务闭环 |
| Excel 导入 | 支持 `.xlsx` / `.xlsm`，可解析年度 Sheet、黄色无效行、金额字段、表头校验 | 可用于导入脱敏模板数据 |
| Excel 导出 | 支持按项目 ID 和按查询条件导出 | 可用于前端下载 |

重要差异：原 V2 文档中“综合查询”曾写为 `GET /api/projects/search`，当前代码实现为 `POST /api/projects/search`。联调以当前代码为准。

## 3. 总体架构

```text
浏览器
  |
  | Vue 页面 / Pinia 状态 / Axios API 封装
  v
Vite 开发代理 /api -> FastAPI
  |
  | 路由层：auth、projects、metadata、import、audit、query-history
  v
Service 层：认证、查询、导入、导出、审计、历史
  |
  v
SQLite/PostgreSQL 数据库 + uploads/exports 文件目录
```

开发期推荐通过 Vite proxy 转发 `/api`，避免浏览器跨域。生产期可以由 Nginx 或同源部署统一反向代理。

## 4. 职责边界

| 范围 | 前端负责 | 后端负责 |
|---|---|---|
| 页面、路由、交互、状态管理 | 是 | 否 |
| Token 保存、请求头、401 跳转 | 是 | 提供认证接口与鉴权结果 |
| 查询参数组装 | 是 | 校验参数并执行数据库查询 |
| Excel 解析、黄色识别、金额解析 | 否 | 是 |
| 无效原因分类 | 展示中文标签 | 识别并返回枚举值 |
| 导出文件下载 | 触发请求并保存文件 | 生成 Excel 文件 |
| 查询历史和审计日志 | 展示和触发 | 持久化与分页查询 |
| 权限控制 | 菜单和路由展示控制 | 接口鉴权和角色校验 |

## 5. 环境配置

### 5.1 前端配置

开发期真实接口模式建议在 `frontend/.env.local` 中配置：

```env
VITE_API_BASE_URL=/api
VITE_BACKEND_TARGET=http://localhost:8000
VITE_APP_TITLE=项目清单筛选查询插件
VITE_PAGE_SIZE=20
VITE_ENABLE_MOCK=false
```

mock 模式使用 `frontend/.env.mock`，并将 `VITE_ENABLE_MOCK=true`。

### 5.2 后端配置

后端默认配置来自 `backend/app/core/config.py`：

| 配置项 | 开发默认值 | 说明 |
|---|---|---|
| `APP_NAME` | `project-list-plugin` | FastAPI 应用名称 |
| `DB_URL` | `sqlite:///./dev.db` | 数据库连接 |
| `EXCEL_UPLOAD_DIR` | `./uploads` | 上传文件目录 |
| `EXCEL_EXPORT_DIR` | `./exports` | 导出文件目录 |
| `SECRET_KEY` | `change-me-in-production` | Token 签名密钥 |
| `IMPORT_BATCH_SIZE` | `500` | 批量导入大小 |
| `DEFAULT_ADMIN_USERNAME` | `admin` | 默认管理员 |
| `DEFAULT_ADMIN_PASSWORD` | `admin123456` | 默认管理员密码 |

生产部署必须替换 `SECRET_KEY`，并根据现场环境决定是否从 SQLite 切换到 PostgreSQL。

## 6. 启动与联调方式

### 6.1 启动后端

```powershell
cd d:\白玉京\excel\backend
python scripts\init_db.py
uvicorn app.main:app --reload
```

检查：

```text
GET http://localhost:8000/health
GET http://localhost:8000/docs
```

预期 `/health` 返回：

```json
{"status":"ok"}
```

### 6.2 启动前端真实接口模式

```powershell
cd d:\白玉京\excel\frontend
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```

浏览器访问：

```text
http://127.0.0.1:5173/
```

### 6.3 启动前端 mock 模式

```powershell
cd d:\白玉京\excel\frontend
npm.cmd run dev:mock
```

mock 模式不依赖后端，适合页面演示和前端自测。

## 7. API 总览

所有业务接口统一带 `/api` 前缀。

| 功能 | 方法 | 路径 | 鉴权 | 前端入口 |
|---|---|---|---|---|
| 登录 | POST | `/api/auth/login` | 否 | 登录页 |
| 当前用户 | GET | `/api/auth/me` | 是 | 路由守卫、顶部用户 |
| 项目查询 | POST | `/api/projects/search` | 是 | 查询页 |
| 项目详情 | GET | `/api/projects/{project_id}` | 是 | 详情弹窗 |
| 按 ID 导出 | POST | `/api/projects/export` | 是 | 勾选后导出 |
| 按查询导出 | POST | `/api/projects/export-by-query` | 是 | 导出当前查询 |
| 项目复核 | POST | `/api/projects/{project_id}/review` | 是 | 复核弹窗 |
| 采购人列表 | GET | `/api/purchasers` | 否/可放开 | 查询表单下拉 |
| 无效统计 | GET | `/api/stats/invalid` | 否/可放开 | 查询页统计 |
| 数据导入 | POST | `/api/import/upload` | 是，admin | 导入页 |
| 查询历史列表 | GET | `/api/query-history` | 是 | 查询历史抽屉 |
| 保存查询历史 | POST | `/api/query-history` | 是 | 查询成功后 |
| 删除查询历史 | DELETE | `/api/query-history/{history_id}` | 是 | 查询历史抽屉 |
| 审计日志 | GET | `/api/audit/logs` | 是，admin | 审计页 |

~~已补强：`/api/import/upload`、`/api/audit/logs` 已显式校验 admin 角色，避免只依赖前端隐藏入口。~~

## 8. 核心接口契约

### 8.1 登录

```http
POST /api/auth/login
Content-Type: application/json
```

请求：

```json
{
  "username": "admin",
  "password": "admin123456"
}
```

响应：

```json
{
  "success": true,
  "data": {
    "access_token": "token",
    "token_type": "bearer",
    "user": {
      "id": 1,
      "username": "admin",
      "display_name": "管理员",
      "role": "admin",
      "is_active": true
    }
  }
}
```

前端处理：

- 保存 `access_token` 到 `localStorage`。
- 后续请求统一带 `Authorization: Bearer <token>`。
- 任一接口返回 401 时清理本地 token 并跳转 `/login`。

### 8.2 项目查询

```http
POST /api/projects/search
Authorization: Bearer <token>
Content-Type: application/json
```

前端默认请求：

```json
{
  "keyword": null,
  "code": null,
  "purchaser": null,
  "date_from": null,
  "date_to": null,
  "amount_min": null,
  "amount_max": null,
  "amount_field": "any",
  "invalid_mode": "valid_only",
  "invalid_reason": null,
  "page": 1,
  "page_size": 20,
  "sort_by": "bid_open_date",
  "sort_order": "desc"
}
```

响应：

```json
{
  "success": true,
  "data": {
    "items": [],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 0
    }
  }
}
```

注意：

- 后端 schema 默认 `invalid_mode = all`，但业务需求要求默认隐藏无效数据，因此前端必须显式传 `valid_only`。
- `page_size` 最大为 100。
- `sort_by` 只允许：`bid_open_date`、`sheet_year`、`project_code`、`purchaser`、`created_at`、`project_name`。

### 8.3 项目字段

```ts
interface ProjectRead {
  id: number
  seq_no?: number | null
  project_name: string
  project_code?: string | null
  purchaser?: string | null
  bid_open_date?: string | null
  commission_amount?: string | null
  commission_num?: string | null
  max_price?: string | null
  max_price_num?: string | null
  bid_amount?: string | null
  bid_amount_num?: string | null
  bid_amount_detail?: string | null
  sheet_year: number
  is_invalid: boolean
  invalid_reason?: string | null
}
```

展示规则：

- 表格金额展示原文字段：`commission_amount`、`max_price`、`bid_amount`。
- `*_num` 字段只用于筛选和必要时调试，不作为主展示内容。
- `is_invalid = true` 的行使用浅黄色背景。
- `invalid_reason` 由前端映射为中文标签。

### 8.4 枚举映射

无效模式：

| 值 | 中文 | SQL 语义 |
|---|---|---|
| `valid_only` | 仅有效 | `is_invalid = false` |
| `all` | 全部 | 不加无效条件 |
| `invalid_only` | 仅无效 | `is_invalid = true` |

金额字段：

| 值 | 中文 | 字段 |
|---|---|---|
| `any` | 任一金额 | 三个金额数字字段任一满足 |
| `commission` | 委托金额 | `commission_num` |
| `max_price` | 最高限价 | `max_price_num` |
| `bid_amount` | 中标金额 | `bid_amount_num` |

无效原因：

| 值 | 中文 |
|---|---|
| `BID_FAILED` | 废标 |
| `TERMINATED` | 终止采购 |
| `CANCELLED` | 采购取消 |
| `WINNER_QUIT` | 中标人放弃 |
| `BREACH` | 履约失败 |
| `OTHER` | 其他 |

### 8.5 导出

按 ID 导出：

```http
POST /api/projects/export
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "project_ids": [1, 2, 3],
  "file_name": "查询结果.xlsx"
}
```

按查询条件导出：

```http
POST /api/projects/export-by-query
Authorization: Bearer <token>
Content-Type: application/json
```

请求体与项目查询一致，可额外带：

```json
{
  "file_name": "查询结果.xlsx"
}
```

响应为 Excel Blob，前端使用 `responseType: "blob"` 下载。

### 8.6 数据导入

```http
POST /api/import/upload
Content-Type: multipart/form-data
```

字段：

```text
file=<xlsx 或 xlsm 文件>
```

响应：

```json
{
  "success": true,
  "data": {
    "file_name": "项目清单-脱敏模板.xlsx",
    "total_rows": 423,
    "imported_rows": 423,
    "valid_rows": 336,
    "invalid_rows": 87,
    "skipped_rows": 0,
    "sheet_stats": [],
    "warnings": []
  }
}
```

导入规则承接 V2 文档：

- 按 Sheet 名解析年度。
- 按表头定位 9 个核心字段。
- 黄色背景为主、关键词为辅识别无效数据。
- 金额字段采用“原文 + 数字”双字段。
- 解析失败记录 warning，不中断整体导入。

### 8.7 查询历史

列表：

```http
GET /api/query-history?page=1&page_size=20
```

保存：

```http
POST /api/query-history
```

```json
{
  "query_params": {
    "keyword": "医院",
    "invalid_mode": "valid_only"
  },
  "result_count": 20
}
```

删除：

```http
DELETE /api/query-history/{history_id}
```

建议前端在查询成功后保存历史；点击历史项时回填查询条件并重新执行查询。

### 8.8 审计日志

```http
GET /api/audit/logs?page=1&page_size=20&action=search
```

返回字段：

| 字段 | 说明 |
|---|---|
| `id` | 日志 ID |
| `user_id` | 操作用户 |
| `action` | 操作类型 |
| `detail` | JSON 详情 |
| `ip_address` | 来源 IP |
| `created_at` | 操作时间 |

当前已记录登录、导出、详情查看、复核、查询历史保存/删除等操作；查询操作是否自动落审计以 `ProjectQueryService` 实现为准。

## 9. 前端连接实现要求

### 9.1 HTTP 客户端

`frontend/src/api/http.ts` 为唯一 Axios 实例：

- `baseURL = VITE_API_BASE_URL || "/api"`。
- 请求拦截器自动带 token。
- 响应拦截器处理 401。
- 错误提示优先读取 `detail`，其次读取 `message`。

### 9.2 页面与接口关系

| 页面/组件 | API |
|---|---|
| `LoginView.vue` | `/auth/login`、`/auth/me` |
| `SearchView.vue` | `/projects/search`、`/purchasers`、`/stats/invalid` |
| `ProjectResultTable.vue` | `/projects/{id}`、`/projects/export`、`/projects/export-by-query` |
| `ProjectDetailDialog.vue` | `/projects/{id}` |
| `ReviewDialog.vue` | `/projects/{id}/review` |
| `QueryHistoryDrawer.vue` | `/query-history` |
| `ImportView.vue` | `/import/upload` |
| `AuditLogView.vue` | `/audit/logs` |

### 9.3 前端默认值

前端默认查询条件必须保持：

```json
{
  "amount_field": "any",
  "invalid_mode": "valid_only",
  "page": 1,
  "page_size": 20,
  "sort_by": "bid_open_date",
  "sort_order": "desc"
}
```

重置筛选时也应恢复上述默认值。

## 10. 后端连接实现要求

### 10.1 路由层

`backend/app/main.py` 统一挂载：

```text
/api/import
/api/projects
/api/purchasers
/api/stats/invalid
/api/auth
/api/audit
/api/query-history
```

### 10.2 Service 层

| Service | 职责 |
|---|---|
| `AuthService` | 默认用户、登录校验、token 生成 |
| `ProjectQueryService` | 查询条件拼接、分页、排序、导出集合 |
| `ProjectRepository` | 项目基础查询、采购人、无效统计、批量写入 |
| `ExcelImporter` | Excel 解析与入库 |
| `excel_exporter` | Excel 导出 |
| `AuditService` | 审计写入与列表 |
| `QueryHistoryService` | 查询历史保存、列表、删除 |

### 10.3 数据一致性要求

- 导入不修改原始 Excel 文件。
- `project_code` 不能作为全局唯一业务假设，实际去重以当前 repository 逻辑为准。
- 查询和导出必须使用同一套筛选逻辑，避免页面结果与导出结果不一致。
- 无效数据在查询、详情、导出中都必须保留 `is_invalid` 与 `invalid_reason`。

## 11. 联调验收流程

### 11.1 基础连通

1. 后端启动成功，`GET /health` 返回 `{"status":"ok"}`。
2. 前端真实接口模式启动成功。
3. 浏览器打开登录页。
4. 使用 `admin / admin123456` 登录成功。
5. 刷新页面后仍能通过 `/auth/me` 恢复用户状态。

### 11.2 数据导入

1. admin 进入数据导入页。
2. 上传脱敏模板 Excel。
3. 后端返回导入统计。
4. 前端展示总行数、导入行数、有效/无效行数、warnings。
5. 导入后查询页可以查到数据。

### 11.3 查询主链路

1. 进入查询页自动加载采购人列表和无效统计。
2. 执行默认查询，请求体包含 `invalid_mode: "valid_only"`。
3. 表格展示有效数据，分页 total 正确。
4. 输入关键词后结果变化，项目名称高亮。
5. 切换为“全部”，能看到无效黄色行。
6. 切换为“仅无效”，可按无效原因筛选。
7. 金额范围筛选可分别按委托金额、最高限价、中标金额、任一金额执行。
8. 排序字段只使用白名单字段。

### 11.4 详情、复核、导出

1. 点击详情可打开完整字段弹窗。
2. 对项目提交复核备注，接口成功并提示保存。
3. 勾选多条记录后可按 ID 导出。
4. 当前筛选条件可按查询导出。
5. 导出的 Excel 字段完整，无效行保留黄色背景。

### 11.5 历史与审计

1. 查询成功后保存查询历史。
2. 查询历史抽屉能分页展示近 20 条。
3. 点击历史项可回填并再次查询。
4. 删除历史项后列表刷新。
5. 审计日志页能分页展示操作记录，JSON detail 可查看。

## 12. 测试命令

前端：

```powershell
cd d:\白玉京\excel\frontend
npm.cmd run test
npm.cmd run typecheck
npm.cmd run build
```

后端：

```powershell
cd d:\白玉京\excel\backend
pytest tests -q -p no:cacheprovider
```

如依赖通过 `.deps` 提供：

```powershell
$env:PYTHONPATH='.deps'
$env:PYTHONDONTWRITEBYTECODE='1'
pytest tests -q -p no:cacheprovider
```

### 12.1 当前执行记录

- ~~后端 AST 语法检查已通过：`python -c "ast.parse(...)"`。~~
- ~~后端不依赖数据库/Excel 第三方包的基础单测已通过：`tests/test_amount_parser.py`、`tests/test_invalid_classifier.py`，共 4 个用例。~~
- ~~后端完整 `pytest tests` 已通过：16 passed，17 warnings。~~
- ~~前端 `npm.cmd run typecheck` 已通过。~~
- ~~前端 `npm.cmd run build` 已通过：Vite build completed。~~
- ~~前端 `npm.cmd run test` 已通过：3 个测试文件、9 个用例通过。~~

## 13. 当前风险与处理建议

| 风险 | 影响 | 建议 |
|---|---|---|
| 原文档与代码接口方法不一致 | 联调 404/405 | 以当前代码 `POST /api/projects/search` 为准，并回写文档 |
| ~~后端 schema 默认 `invalid_mode=all`，前端业务默认为 `valid_only`~~ | ~~默认结果包含无效数据~~ | ~~已调整后端默认值为 `valid_only`，前端仍显式传 `valid_only`~~ |
| ~~`/api/import/upload` 当前未强制 admin 鉴权~~ | ~~非 admin 可能绕过页面直接导入~~ | ~~已补 `require_admin` 角色校验，并记录导入审计日志~~ |
| ~~审计日志接口当前仅要求登录~~ | ~~viewer 可能看到审计~~ | ~~已补 admin 角色校验~~ |
| 日期字段质量曾出现异常记录 | 日期筛选、排序、导出结果不可信 | 用新导入数据复测 `bid_open_date`，日期验收单独列为必验项 |
| ~~查询历史保存时机不统一~~ | ~~历史缺失或重复~~ | ~~已明确由后端 `ProjectQueryService.search_projects()` 自动保存查询历史和审计，前端只读取/复用/删除~~ |
| 导出文件名编码 | 浏览器下载文件名乱码 | 前端优先解析 `Content-Disposition`，失败时使用本地默认名 |
| ~~前端 Vitest/Vite 在本机 spawn esbuild 返回 EPERM~~ | ~~无法在当前机器完成 `npm run test`~~ | ~~前端单测已复跑通过：3 个测试文件、9 个用例通过~~ |
| ~~后端完整测试依赖安装较慢~~ | ~~无法在当前机器立刻跑完整 pytest~~ | ~~依赖已手动安装完成，完整后端测试已通过：16 passed~~ |

## 14. 建议开发顺序

1. ~~确认后端可启动、数据库可初始化、默认 admin 可登录。~~
2. ~~用 Swagger 跑通登录、导入、查询、详情、导出。~~
3. ~~前端关闭 mock，确认登录和 `/auth/me` 可用。~~
4. ~~联调查询页，先不带复杂条件，只确认分页和默认有效数据。~~
5. ~~联调无效模式、金额范围、采购人、关键词、排序。~~
6. ~~联调详情、复核、导出。~~
7. ~~联调查询历史和审计日志。~~
8. ~~补角色鉴权。~~ 日期字段验收、E2E 主链路测试仍需基于真实导入数据补做。

## 15. 初版完成标准

本阶段可以认为“前后端已连接”的标准：

- ~~前端真实接口模式下可登录并进入系统。~~
- ~~使用脱敏 Excel 可完成导入并查询出数据。~~
- ~~查询、详情、复核、导出、历史、审计主链路均已接入真实 API。~~
- ~~默认查询不展示无效数据，切换后无效行黄色展示。~~
- ~~导出结果与当前查询条件一致。~~
- 前端 `test/typecheck/build` 通过，后端 `pytest` 通过。
- ~~风险项中“导入鉴权、审计权限”已有处理结论并完成代码补强。~~ 日期字段质量仍需用真实导入数据做最终验收。

## 16. 连接执行清单

- ~~后端新增 `require_admin` 依赖，用于统一校验 admin 角色。~~
- ~~`POST /api/import/upload` 已接入 admin 鉴权，非 admin 不能绕过前端直接导入。~~
- ~~`POST /api/import/upload` 已补导入审计日志，记录文件名、总行数、导入行数、有效/无效行数、warning 数。~~
- ~~`GET /api/audit/logs` 已接入 admin 鉴权，viewer 不能查看审计日志。~~
- ~~后端 `ProjectSearchRequest.invalid_mode` 默认值已改为 `valid_only`，与前端业务默认一致。~~
- ~~前端已新增 `frontend/.env.local`，真实接口模式默认走 `/api -> http://localhost:8000`。~~
- ~~前端默认查询条件已确认：`amount_field=any`、`invalid_mode=valid_only`、`page_size=20`、`sort_by=bid_open_date`、`sort_order=desc`。~~
- ~~后端轻量验证已完成：AST 语法检查通过，金额解析和无效分类基础单测 4 个通过。~~
- ~~前端类型验证已完成：`npm.cmd run typecheck` 通过。~~
- ~~后端完整 `pytest tests` 已复跑通过：16 passed，17 warnings。~~
- ~~前端 `npm.cmd run build` 已通过。~~
- ~~前端 `npm.cmd run test` 已复跑通过：3 个测试文件、9 个用例通过。~~
- 日期字段质量待真实脱敏 Excel 重新导入后验收，重点检查 `bid_open_date` 是否仍有异常年份。
