# 项目清单筛选查询插件 - 前端开发设计文档

版本：V1.1  
日期：2026-05-14  
范围：前端  
参考文档：

- 《需求文档_项目清单筛选查询插件_V2.docx》
- 《开发文档_项目清单筛选查询插件_V2.docx》
- [后端A_开发设计文档.md](./后端A_开发设计文档.md)
- [后端B_开发设计文档.md](./后端B_开发设计文档.md)

## 实施进度

- [x] ~~搭建前端项目骨架、路由、布局、权限守卫与 API 客户端~~
- [x] ~~完成登录页、Token 保存、当前用户获取与退出登录~~
- [x] ~~完成查询主页：筛选表单、采购人下拉、无效数据模式、查询历史入口~~
- [x] ~~完成结果列表：分页表格、排序、关键词高亮、无效行黄色标记、详情弹窗~~
- [x] ~~完成导出、复核、查询历史保存/复用/删除~~
- [x] ~~完成数据导入页、导入结果展示、审计日志页~~
- [x] ~~补充基础单元测试、类型检查与生产构建验证~~
- [ ] 补充前端 mock 测试、组件测试和 E2E 测试
- [x] ~~处理本地开发服务后台运行不稳定问题，确保浏览器可稳定访问~~

## 0. 当前状态与前端依赖

### 0.1 前端当前完成情况

截至 2026-05-14，前端代码已完成以下内容：

- 已创建 `frontend/` Vue 3 + Vite + TypeScript 项目。
- 已完成 Element Plus、Pinia、Vue Router、Axios、lucide 图标接入。
- 已完成登录页、主布局、路由守卫、Token 保存、当前用户恢复和退出登录。
- 已完成查询页、筛选表单、采购人下拉、无效数据模式、查询历史抽屉。
- 已完成结果表格、分页、排序、关键词高亮、无效行黄色标记、项目详情弹窗。
- 已完成导出当前查询、导出选中记录、复核弹窗。
- 已完成 admin 数据导入页、导入结果展示、审计日志页。
- 已生成 `package-lock.json`，依赖已安装到 `frontend/node_modules`。

### 0.2 当前验证记录

已通过的本地验证：

```text
npm.cmd run test       # 3 个测试文件，9 个测试通过
npm.cmd run typecheck  # 通过
npm.cmd run build      # 通过
```

当前验证范围说明：

- 已覆盖 `format`、`highlight`、`projectSearch store` 的基础单元测试。
- 已完成生产构建验证。
- 已新增前端 mock 数据和 `dev:mock` 预览模式。
- 尚未完成接口 mock 自动化测试。
- 尚未完成 Playwright/E2E 测试。
- 真实后端联调属于协作依赖，不计入前端独立完成项。

### 0.3 当前运行状态

当前前端 mock 预览服务已可访问：

- 已新增 `.env.mock` 和 `npm.cmd run dev:mock`，用于前端独立预览。
- 已在沙箱外启动 `npm.cmd run dev:mock`。
- 已确认 `127.0.0.1:5173` 正在监听。
- 已通过命令行请求 `http://127.0.0.1:5173/`，返回 HTTP 200。

当前打开方式：

```text
http://127.0.0.1:5173/
```

mock 模式说明：

- 不依赖后端服务，可用于前端页面和交互验收。
- 登录页可输入任意用户名和密码进入系统。
- 查询、详情、复核、导入、审计日志均使用前端 mock 数据。
- 后续接真实后端时，使用 `npm.cmd run dev` 并确保后端服务可用。

### 0.4 当前后端状态与前端依赖

截至 2026-05-14，后端 A/B 已提供可供前端联调的主要接口：

- 登录：`POST /api/auth/login`
- 当前用户：`GET /api/auth/me`
- 项目查询：`POST /api/projects/search`
- 项目详情：`GET /api/projects/{project_id}`
- 按 ID 导出：`POST /api/projects/export`
- 按查询条件导出：`POST /api/projects/export-by-query`
- 项目复核：`POST /api/projects/{project_id}/review`
- 采购人列表：`GET /api/purchasers`
- 无效数据统计：`GET /api/stats/invalid`
- 查询历史：`GET /api/query-history`、`POST /api/query-history`、`DELETE /api/query-history/{history_id}`
- 审计日志：`GET /api/audit/logs`
- Excel 导入：`POST /api/import/upload`

当前已知后端限制：

1. `bid_open_date` 导入结果曾出现异常，前端日期筛选和日期展示需要等后端 A 修正后做最终验收。
2. 用户管理接口暂未在当前代码中提供，前端本期不实现用户管理页，只保留路由/菜单扩展位。
3. 导入接口当前代码未强制鉴权，前端仍按 admin 权限入口设计，避免后续后端补鉴权时改动页面逻辑。

### 0.5 前后端责任边界

本文档只跟踪前端负责范围。

| 类别 | 是否属于前端负责 | 说明 |
|---|---|---|
| 页面开发 | 是 | 登录、查询、结果列表、详情、复核、导入、审计日志等页面 |
| 前端路由与权限展示 | 是 | 基于后端返回用户角色控制菜单和路由访问 |
| API 客户端封装 | 是 | 按后端契约封装请求、响应、错误处理和文件下载 |
| 前端 mock / 组件测试 / E2E | 是 | 用 mock 数据验证前端页面和交互 |
| 启动和修复后端服务 | 否 | 后端团队或后端负责人处理 |
| 修改后端接口、数据库、导入逻辑 | 否 | 前端只记录依赖和风险，不直接修复 |
| 真实后端联调验收 | 协作项 | 后端环境可用后，前端配合验证请求响应和页面表现 |

因此，后续实施进度中的“未完成”只表示前端自身还需要收口的工作；后端服务未启动、后端接口异常、后端日期字段异常不作为前端开发未完成项。

## 1. 前端定位

前端负责把后端 A/B 提供的数据导入、查询、导出、审计能力包装成业务人员可直接使用的 Web 界面，目标是替代业务人员直接操作 Excel 的复杂筛选动作。

本阶段前端主线为：

```text
登录 -> 输入筛选条件 -> 调用查询 API -> 表格展示结果 -> 无效数据视觉区分 -> 查看详情/复核 -> 导出结果 -> 保存并复用查询历史
```

前端设计重点：

- 查询入口简单清晰，适合不熟悉 Excel 操作的业务人员。
- 默认隐藏无效数据，允许切换显示全部或仅无效。
- 无效数据行保留黄色视觉标记，与原始 Excel 认知一致。
- 筛选项、表格、分页、导出入口与后端接口语义保持稳定。
- admin 页面与 viewer 页面按角色区分显示。

## 2. 分工边界

| 角色 | 主责 | 前端处理方式 |
|---|---|---|
| 后端 A | Excel 导入、清洗、金额解析、无效识别、导出底层能力 | 前端只展示导入结果和导出文件，不重复解析 Excel |
| 后端 B | 查询、认证、审计、查询历史、复核、导出主流程 | 前端按后端 B 的请求/响应结构封装 API |
| 前端 | 页面、交互、状态管理、错误提示、前端自测、配合联调 | 不直接访问数据库，不在前端重写业务筛选规则 |

前端不负责：

- 不解析 Excel 单元格颜色。
- 不判断金额字段是否有效。
- 不自行计算无效原因。
- 不绕过后端权限直接构造管理能力。

## 3. 技术架构

### 3.1 推荐技术栈

| 模块 | 技术 | 说明 |
|---|---|---|
| 构建工具 | Vite | Vue 3 标准轻量构建方案 |
| 前端框架 | Vue 3 + TypeScript | 组合式 API，类型约束接口字段 |
| UI 组件库 | Element Plus | 表单、表格、上传、分页、弹窗场景成熟 |
| 路由 | Vue Router | 登录页、主查询页、管理页分离 |
| 状态管理 | Pinia | 用户态、查询条件、元数据缓存 |
| HTTP 客户端 | Axios | 统一 Token、错误处理、文件下载 |
| 日期处理 | dayjs | 日期格式化和日期范围处理 |
| 测试 | Vitest + Vue Test Utils | 组件和工具函数测试 |
| E2E | Playwright | 关键业务链路验收 |

### 3.2 目录结构建议

```text
frontend/
  src/
    main.ts
    App.vue
    router/
      index.ts
      guards.ts
    api/
      http.ts
      auth.ts
      projects.ts
      metadata.ts
      import.ts
      audit.ts
      queryHistory.ts
    stores/
      auth.ts
      projectSearch.ts
      metadata.ts
    views/
      LoginView.vue
      SearchView.vue
      ImportView.vue
      AuditLogView.vue
    layouts/
      AppLayout.vue
    components/
      search/
        ProjectSearchForm.vue
        QueryHistoryDrawer.vue
      projects/
        ProjectResultTable.vue
        ProjectDetailDialog.vue
        ReviewDialog.vue
      import/
        ExcelUploadPanel.vue
        ImportResultPanel.vue
      common/
        PageToolbar.vue
        EmptyState.vue
    types/
      auth.ts
      project.ts
      query.ts
      audit.ts
      import.ts
    utils/
      download.ts
      format.ts
      highlight.ts
    styles/
      variables.css
      global.css
    tests/
```

如果当前仓库继续采用单仓结构，建议将前端放在仓库根目录的 `frontend/` 下，与 `backend/` 平级。

## 4. 配置设计

前端所有环境差异从 `.env` 读取，不在业务代码中写死。

| 配置项 | 开发默认值 | 生产示例 | 用途 |
|---|---|---|---|
| VITE_API_BASE_URL | /api | http://内网IP:8000/api | 后端 API 地址，开发期默认走 Vite proxy |
| VITE_BACKEND_TARGET | http://localhost:8000 | 不需要 | Vite 开发代理目标 |
| VITE_APP_TITLE | 项目清单筛选查询插件 | 项目清单筛选查询插件 | 页面标题 |
| VITE_PAGE_SIZE | 20 | 20 | 查询表格默认分页 |
| VITE_ENABLE_MOCK | false | false | 是否启用本地 mock |

`.env.example` 示例：

```env
VITE_API_BASE_URL=/api
VITE_BACKEND_TARGET=http://localhost:8000
VITE_APP_TITLE=项目清单筛选查询插件
VITE_PAGE_SIZE=20
VITE_ENABLE_MOCK=false
```

说明：

- 前端当前 `vite.config.ts` 已配置 `/api` 代理到 `VITE_BACKEND_TARGET`。
- 开发期推荐保留 `VITE_API_BASE_URL=/api`，避免浏览器跨域问题。
- 生产部署时可根据实际部署方式改成后端完整地址或由 Nginx 统一反向代理。

## 5. 页面与路由设计

### 5.1 路由表

| 路由 | 页面 | 权限 | 说明 |
|---|---|---|---|
| `/login` | 登录页 | anonymous | 用户名密码登录 |
| `/` | 查询主页 | viewer/admin | 默认跳转查询主页 |
| `/search` | 查询主页 | viewer/admin | 筛选表单 + 结果列表 |
| `/import` | 数据导入页 | admin | 上传 Excel、查看导入统计 |
| `/audit/logs` | 审计日志页 | admin | 查询操作日志 |

暂不实现但预留：

| 路由 | 页面 | 说明 |
|---|---|---|
| `/users` | 用户管理页 | 等后端提供用户管理接口后实现 |

### 5.2 页面布局

采用后台工具型布局：

- 顶部栏：系统名称、当前用户、角色、退出登录。
- 左侧菜单：查询、数据导入、审计日志；admin 才显示管理菜单。
- 主内容区：承载查询表单、表格、导入和审计页面。

查询页面不做营销式首页，首屏直接展示可用的筛选和结果区域。

## 6. 核心页面设计

### 6.1 P01 登录页

功能：

- 输入用户名、密码。
- 调用 `POST /api/auth/login`。
- 保存 `access_token`、用户信息。
- 登录成功后跳转 `/search`。
- 登录失败显示明确错误提示。

前端存储：

- Token：`localStorage`。
- 用户信息：Pinia + 刷新后通过 `GET /api/auth/me` 恢复。

### 6.2 P02 查询主页

查询表单字段：

| UI 字段 | 后端字段 | 控件 |
|---|---|---|
| 项目名称关键词 | keyword | 输入框 |
| 项目编号 | code | 输入框 |
| 采购人 | purchaser | 可搜索下拉 |
| 开标时间 | date_from / date_to | 日期范围选择器 |
| 金额范围 | amount_min / amount_max | 数字输入框 |
| 金额字段 | amount_field | 单选/分段控件：任一、委托金额、最高限价、中标金额 |
| 无效数据 | invalid_mode | 三态分段控件：仅有效、全部、仅无效 |
| 无效原因 | invalid_reason | 下拉，仅 `invalid_only` 时启用或重点展示 |

默认值建议：

```json
{
  "keyword": null,
  "code": null,
  "purchaser": null,
  "date_from": null,
  "date_to": null,
  "amount_min": null,
  "amount_max": null,
  "amount_field": "any",
  "invalid_mode": "valid_only",
  "invalid_reason": null,
  "page": 1,
  "page_size": 20,
  "sort_by": "bid_open_date",
  "sort_order": "desc"
}
```

说明：

- 后端 schema 当前默认 `invalid_mode` 为 `all`，但需求文档要求默认隐藏无效数据。前端初始化查询条件应显式传 `valid_only`。
- 点击“重置”后恢复上述默认值。
- 采购人下拉从 `GET /api/purchasers` 获取，并在前端做本地搜索。
- 无效原因中文选项由前端固定枚举映射，后端仍传枚举值。

### 6.3 P03 结果列表

表格字段：

| 列名 | 字段 | 展示规则 |
|---|---|---|
| 序号 | seq_no | 空值显示 `-` |
| 项目名称 | project_name | 支持关键词高亮 |
| 项目编号 | project_code | 支持复制 |
| 采购人 | purchaser | 空值显示 `-` |
| 开标时间 | bid_open_date | `YYYY-MM-DD`，空值显示 `-` |
| 委托金额 | commission_amount | 展示原文 |
| 最高限价 | max_price | 展示原文 |
| 中标金额 | bid_amount | 展示原文 |
| 所属年度 | sheet_year | 数字 |
| 无效原因 | invalid_reason | 中文标签 |
| 操作 | id | 详情、复核 |

交互要求：

- 表格支持分页。
- 表格支持后端排序字段：`bid_open_date`、`sheet_year`、`project_code`、`purchaser`、`created_at`、`project_name`。
- `is_invalid = true` 的行使用浅黄色背景。
- 关键词高亮只做展示增强，不改变查询逻辑。
- 查询结果顶部显示总数；如果当前页存在无效数据，显示无效提示。

### 6.4 P04 项目详情弹窗

触发：

- 点击表格“详情”。
- 调用 `GET /api/projects/{project_id}` 获取详情。

展示：

- 按原始 Excel 字段展示完整信息。
- 单独展示系统字段：所属年度、是否无效、无效原因。
- 金额展示使用原文字段，解析数字字段可折叠展示，默认不面向业务用户突出。

### 6.5 P05 复核弹窗

触发：

- 点击表格“复核”或详情弹窗内“复核”。

请求：

```http
POST /api/projects/{project_id}/review
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "comment": "已确认该项目记录无误"
}
```

说明：

- 当前后端只记录审计日志，不返回持久复核状态。
- 前端提交成功后提示“复核记录已保存”。
- 不在表格中伪造长期复核状态，避免刷新后状态丢失造成误解。

### 6.6 P06 查询历史侧栏

接口：

- `GET /api/query-history?page=1&page_size=20`
- `POST /api/query-history`
- `DELETE /api/query-history/{history_id}`

设计：

- 查询成功后，可自动或手动保存查询历史。为了避免重复噪音，建议本期采用“查询成功后自动保存”，与后端 B 文档一致。
- 侧栏按时间倒序展示最近 20 条。
- 每条展示摘要：关键词、采购人、日期、金额范围、无效模式、结果数量、查询时间。
- 点击历史项回填表单并重新执行查询。
- 支持删除单条历史。

### 6.7 P07 数据导入页

权限：

- admin 可见。

功能：

- 上传 `.xlsx` / `.xlsm` 文件。
- 调用 `POST /api/import/upload`，使用 `multipart/form-data`。
- 展示导入结果概览：总行数、导入行数、有效行数、无效行数、跳过行数。
- 展示 Sheet 统计和 warnings 列表。
- 上传期间禁用重复提交。

注意：

- 前端只做文件扩展名和大小的基础校验。
- 表头、黄色识别、金额解析错误均以后端返回为准。

### 6.8 P08 审计日志页

权限：

- admin 可见。

接口：

```http
GET /api/audit/logs?page=1&page_size=20&action=search
```

功能：

- 按操作类型过滤。
- 展示操作人、动作、详情、IP、时间。
- `detail` 为 JSON 时使用格式化展示，长内容可弹窗查看。

## 7. API 对接设计

### 7.1 HTTP 客户端

统一封装 `src/api/http.ts`：

- 请求前自动添加 `Authorization: Bearer <token>`。
- 401 时清理登录态并跳转 `/login`。
- 业务错误统一读取 `detail` 或 `message` 提示。
- 文件下载接口设置 `responseType: "blob"`。
- 所有 API 函数返回类型化数据。

### 7.2 登录

```http
POST /api/auth/login
```

请求：

```json
{
  "username": "admin",
  "password": "******"
}
```

响应：

```json
{
  "success": true,
  "data": {
    "access_token": "token",
    "token_type": "bearer",
    "user": {
      "id": 1,
      "username": "admin",
      "display_name": "管理员",
      "role": "admin",
      "is_active": true
    }
  }
}
```

### 7.3 项目查询

```http
POST /api/projects/search
```

请求：

```json
{
  "keyword": "医院",
  "code": "P2026",
  "purchaser": "某采购人",
  "date_from": "2026-01-01",
  "date_to": "2026-12-31",
  "amount_min": 100,
  "amount_max": 500,
  "amount_field": "any",
  "invalid_mode": "valid_only",
  "invalid_reason": null,
  "page": 1,
  "page_size": 20,
  "sort_by": "bid_open_date",
  "sort_order": "desc"
}
```

响应：

```json
{
  "success": true,
  "data": {
    "items": [],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 0
    }
  }
}
```

### 7.4 项目字段类型

```ts
export interface ProjectRead {
  id: number
  seq_no?: number | null
  project_name: string
  project_code?: string | null
  purchaser?: string | null
  bid_open_date?: string | null
  commission_amount?: string | null
  commission_num?: string | null
  max_price?: string | null
  max_price_num?: string | null
  bid_amount?: string | null
  bid_amount_num?: string | null
  bid_amount_detail?: string | null
  sheet_year: number
  is_invalid: boolean
  invalid_reason?: InvalidReason | null
}
```

### 7.5 枚举映射

无效原因：

| 枚举值 | 中文展示 |
|---|---|
| BID_FAILED | 废标 |
| TERMINATED | 终止采购 |
| CANCELLED | 采购取消 |
| WINNER_QUIT | 中标人放弃 |
| BREACH | 履约失败 |
| OTHER | 其他 |

金额字段：

| 枚举值 | 中文展示 |
|---|---|
| any | 任一金额 |
| commission | 委托金额 |
| max_price | 最高限价 |
| bid_amount | 中标金额 |

无效模式：

| 枚举值 | 中文展示 | 前端默认 |
|---|---|---|
| valid_only | 仅有效数据 | 是 |
| all | 全部数据 | 否 |
| invalid_only | 仅无效数据 | 否 |

## 8. 状态管理设计

### 8.1 auth store

保存：

- `token`
- `user`
- `isAuthenticated`
- `isAdmin`

方法：

- `login(payload)`
- `fetchCurrentUser()`
- `logout()`
- `restoreFromStorage()`

### 8.2 projectSearch store

保存：

- 当前查询条件
- 当前结果列表
- 分页信息
- loading 状态
- 当前选中的项目详情

方法：

- `search(resetPage?: boolean)`
- `resetFilters()`
- `changePage(page)`
- `changeSort(sort_by, sort_order)`
- `loadDetail(projectId)`
- `exportByQuery()`
- `exportSelected(projectIds)`

### 8.3 metadata store

保存：

- 采购人列表
- 无效统计
- 枚举选项

方法：

- `loadPurchasers()`
- `loadInvalidStats()`

## 9. 权限与路由守卫

路由守卫规则：

1. 未登录访问业务页面，跳转 `/login`。
2. 已登录访问 `/login`，跳转 `/search`。
3. 非 admin 访问 `/import`、`/audit/logs`，提示无权限并跳转 `/search`。
4. Token 过期或接口返回 401，清理本地登录态并跳转 `/login`。

角色权限：

| 角色 | 查询 | 导出 | 复核 | 数据导入 | 审计日志 | 用户管理 |
|---|---|---|---|---|---|---|
| viewer | 是 | 是 | 是 | 否 | 否 | 否 |
| admin | 是 | 是 | 是 | 是 | 是 | 预留 |

## 10. 交互与视觉规范

### 10.1 总体风格

本系统是业务查询工具，视觉应安静、清晰、适合长时间使用：

- 采用高密度但不拥挤的表单和表格布局。
- 避免营销式大标题、装饰性大图和过度渐变。
- 优先保证查询、阅读、比较、导出这些高频任务效率。

### 10.2 无效数据视觉

- 表格行背景使用浅黄色，例如 `#fff7cc`。
- 无效原因使用标签展示，但颜色保持克制。
- 鼠标悬浮时仍保留可辨识黄色底，不被 hover 色完全覆盖。

### 10.3 反馈规则

| 场景 | 前端反馈 |
|---|---|
| 查询中 | 表格 loading |
| 无结果 | 空状态提示，可保留筛选条件 |
| 导出中 | 按钮 loading，禁止重复点击 |
| 导出成功 | 自动下载文件 |
| 导入中 | 上传组件进度/全局 loading |
| 导入有 warning | 在结果面板中展示 warning 列表 |
| 复核成功 | 成功提示 |
| 401 | 清理登录态并跳转登录 |

## 11. 数据展示与格式化

### 11.1 日期

- 标准日期显示为 `YYYY-MM-DD`。
- 空日期显示 `-`。
- 后端日期字段修正前，前端不自行猜测或修正异常年份。

### 11.2 金额

- 表格展示原文字段：`commission_amount`、`max_price`、`bid_amount`。
- 筛选使用数字输入，单位默认“万元”。
- `*_num` 字段只用于调试或详情折叠区域，不作为主展示字段。

### 11.3 空值

- 文本空值统一展示 `-`。
- 数字空值统一展示 `-`。
- 不把 `null`、`undefined`、空字符串直接暴露给用户。

## 12. 测试计划与当前验证状态

### 12.1 单元测试

| 测试对象 | 覆盖点 |
|---|---|
| format utils | 已覆盖日期、空值、无效原因中文映射、查询摘要 |
| highlight utils | 已覆盖关键词高亮、空关键词、HTML 转义 |
| projectSearch store | 已覆盖默认条件、重置、历史回填 |
| auth store | 待补登录成功、登录失败、退出、恢复登录态 |
| route guards | 待补未登录跳转、admin 权限、viewer 拦截 |

### 12.2 组件测试

| 组件 | 覆盖点 |
|---|---|
| ProjectSearchForm | 默认值、重置、提交、无效原因启停 |
| ProjectResultTable | 行渲染、无效行黄色背景、排序事件、分页事件 |
| ProjectDetailDialog | 字段展示、空值处理、无效原因展示 |
| QueryHistoryDrawer | 历史列表、回填、删除 |
| ExcelUploadPanel | 文件类型校验、上传成功、上传失败 |

当前状态：组件测试尚未补充。

### 12.3 E2E 测试

建议至少覆盖：

1. 登录成功后进入查询页。
2. 默认查询请求携带 `invalid_mode = valid_only`。
3. 输入关键词后返回结果并高亮。
4. 切换“全部数据”后无效行以黄色展示。
5. 点击详情可查看完整字段。
6. 点击复核可提交备注。
7. 点击导出可下载 Excel 文件。
8. admin 可进入导入页和审计页，viewer 不可进入。

当前状态：E2E 测试尚未补充。

### 12.4 联调验收

使用真实脱敏数据联调：

- 采购人下拉可加载真实采购人。
- 查询响应时间在当前数据量下明显低于 2 秒。
- 默认结果不包含无效数据。
- 切换仅无效后可看到黄色行和中文无效原因。
- 导出结果字段完整，且无效行保留黄色标记。
- 查询历史可复用，回填后能重新执行。

日期筛选最终验收需等待后端 A 修正 `bid_open_date` 后补做。

当前状态：前端 mock 预览服务已可通过 `http://127.0.0.1:5173/` 打开。真实后端联调属于协作项，待后端环境可用后配合验证。

## 13. 实施顺序

建议按以下顺序开发：

1. 创建 `frontend/` 项目骨架，配置 Vite、Vue 3、TypeScript、Element Plus。
2. 封装 Axios 客户端、环境变量、统一错误处理。
3. 实现登录页、auth store、路由守卫和基础布局。
4. 实现项目查询 API、查询 store、查询表单。
5. 实现结果表格、分页、排序、无效行标记和关键词高亮。
6. 实现项目详情、复核弹窗和导出功能。
7. 实现采购人元数据、无效统计、查询历史侧栏。
8. 实现 admin 数据导入页和审计日志页。
9. 补充单元测试、组件测试和 E2E 主链路。
10. 后端环境可用后，配合后端真实 SQLite 数据做全流程联调记录。

## 14. 风险与处理

| 风险 | 表现 | 前端处理方式 |
|---|---|---|
| 后端日期字段异常 | 日期筛选和排序结果不可信 | 前端保留能力，最终验收等待后端修正 |
| 后端接口路径与早期文档不一致 | 联调 404 或参数不匹配 | 以前端 API 封装对齐当前代码中的 `POST /api/projects/search` 等真实接口 |
| 默认无效模式口径差异 | 后端默认 all，需求要求默认隐藏无效 | 前端初始化和重置时显式传 `valid_only` |
| 用户管理接口缺失 | 用户管理页无法实现 | 本期不实现用户管理页，仅保留菜单扩展位 |
| 查询历史过多 | 侧栏噪音大 | 只展示最近 20 条，提供删除 |
| 导出文件名解析差异 | 下载文件名乱码或丢失 | 优先读取响应头文件名，失败时使用本地兜底文件名 |
| Token 过期 | 页面操作突然失败 | 401 统一退出并回登录页 |
| Excel 上传耗时 | 用户重复点击 | 上传期间禁用按钮并展示 loading |

## 15. 本阶段交付物

前端本阶段交付状态：

- [x] 可构建的 Vue 3 前端项目。
- [x] `.env.example` 前端配置模板。
- [x] 登录、查询、结果列表、详情、复核、导出主链路代码。
- [x] 查询历史展示、复用、删除代码。
- [x] admin 数据导入页代码。
- [x] admin 审计日志页代码。
- [x] 与后端 B 当前接口一致的 API 封装和 TypeScript 类型。
- [x] 工具函数和查询 store 的基础单元测试。
- [x] 可稳定打开的前端 mock 本地开发服务。
- [ ] 前端 mock 测试、组件测试和 E2E 测试。
- [x] 前端本地服务稳定访问说明。
- [ ] 待后端环境可用后的联调记录。

## 16. 当前结论

前端主要功能代码已经完成，且已通过 `test`、`typecheck`、`build`。当前前端已可用 mock 模式独立打开页面：

1. 使用 `npm.cmd run dev:mock` 启动前端独立预览。
2. 浏览器访问 `http://127.0.0.1:5173/` 查看页面。
3. 后续补充前端 mock 自动化测试、组件测试和 E2E 测试。
4. 后端环境可用后，再配合完成登录、查询、导入、导出、审计的真实接口联调。
