# 项目清单筛选查询插件 - 前后端联调说明

版本：V1.0  
日期：2026-05-14  
范围：前端联调说明  

## 1. 责任边界

| 项目 | 前端负责 | 后端负责 |
|---|---|---|
| 页面、路由、交互、样式 | 是 | 否 |
| 前端 mock 预览 | 是 | 否 |
| API 客户端封装 | 是 | 配合接口契约 |
| 后端服务启动、数据库、导入逻辑 | 否 | 是 |
| 真实接口联调 | 配合验证页面表现 | 提供可用接口和数据 |

前端可以独立使用 mock 模式验收页面；真实联调需要后端服务可用。

## 2. 前端独立预览

不依赖后端，适合前端开发、页面验收、演示交互。

```powershell
cd E:\vscode\excel查找\excel\frontend
npm.cmd run dev:mock
```

浏览器打开：

```text
http://127.0.0.1:5173/
```

mock 登录：

```text
用户名：admin
密码：admin123456
```

说明：

- mock 模式实际可以输入任意用户名和密码。
- 查询、详情、复核、导入、审计日志均使用前端 mock 数据。
- mock 模式配置文件为 [frontend/.env.mock](./frontend/.env.mock)。

## 3. 真实后端联调

### 3.1 启动后端

由后端负责人在 `backend/` 目录启动服务。

```powershell
cd E:\vscode\excel查找\excel\backend
uvicorn app.main:app --reload
```

默认后端地址：

```text
http://localhost:8000
```

检查接口：

```text
GET http://localhost:8000/health
GET http://localhost:8000/docs
```

默认账号：

```text
用户名：admin
密码：admin123456
```

### 3.2 启动前端真实接口模式

前端真实接口模式不使用 mock。

```powershell
cd E:\vscode\excel查找\excel\frontend
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```

浏览器打开：

```text
http://127.0.0.1:5173/
```

前端默认通过 Vite proxy 将 `/api` 转发给后端。当前代理目标在 [frontend/vite.config.ts](./frontend/vite.config.ts) 中读取：

```text
VITE_BACKEND_TARGET=http://localhost:8000
```

如后端不在 `localhost:8000`，可新增 `frontend/.env.local`：

```env
VITE_API_BASE_URL=/api
VITE_BACKEND_TARGET=http://后端IP:端口
VITE_APP_TITLE=项目清单筛选查询插件
VITE_PAGE_SIZE=20
VITE_ENABLE_MOCK=false
```

## 4. 联调接口清单

所有真实业务接口默认带 `/api` 前缀。

| 功能 | 方法 | 路径 | 前端页面 |
|---|---|---|---|
| 登录 | POST | `/api/auth/login` | 登录页 |
| 当前用户 | GET | `/api/auth/me` | 路由守卫、顶部用户信息 |
| 项目查询 | POST | `/api/projects/search` | 查询主页 |
| 项目详情 | GET | `/api/projects/{project_id}` | 详情弹窗 |
| 按 ID 导出 | POST | `/api/projects/export` | 导出选中 |
| 按查询导出 | POST | `/api/projects/export-by-query` | 导出当前查询 |
| 项目复核 | POST | `/api/projects/{project_id}/review` | 复核弹窗 |
| 采购人列表 | GET | `/api/purchasers` | 采购人下拉 |
| 无效统计 | GET | `/api/stats/invalid` | 无效分类概览 |
| 查询历史列表 | GET | `/api/query-history` | 查询历史抽屉 |
| 删除查询历史 | DELETE | `/api/query-history/{history_id}` | 查询历史抽屉 |
| 数据导入 | POST | `/api/import/upload` | 数据导入页 |
| 审计日志 | GET | `/api/audit/logs` | 审计日志页 |

## 5. 关键请求示例

### 5.1 登录

```http
POST /api/auth/login
Content-Type: application/json
```

```json
{
  "username": "admin",
  "password": "admin123456"
}
```

前端会保存返回的 `access_token`，后续请求自动带：

```http
Authorization: Bearer <token>
```

### 5.2 项目查询

```http
POST /api/projects/search
Authorization: Bearer <token>
Content-Type: application/json
```

```json
{
  "keyword": null,
  "code": null,
  "purchaser": null,
  "date_from": null,
  "date_to": null,
  "amount_min": null,
  "amount_max": null,
  "amount_field": "any",
  "invalid_mode": "valid_only",
  "invalid_reason": null,
  "page": 1,
  "page_size": 20,
  "sort_by": "bid_open_date",
  "sort_order": "desc"
}
```

注意：

- 需求要求默认隐藏无效数据，所以前端默认传 `invalid_mode = valid_only`。
- 后端 schema 虽然默认是 `all`，但以前端显式传参为准。
- `page_size` 最大为 `100`。

### 5.3 数据导入

```http
POST /api/import/upload
Content-Type: multipart/form-data
```

字段：

```text
file=<xlsx 或 xlsm 文件>
```

## 6. 联调验收步骤

1. 前端用 mock 模式确认页面可正常打开。
2. 后端启动 `http://localhost:8000`，确认 `/health` 返回正常。
3. 前端切换真实接口模式，打开 `http://127.0.0.1:5173/`。
4. 使用 `admin / admin123456` 登录。
5. 进入查询页，确认采购人下拉和无效统计能加载。
6. 执行默认查询，确认请求参数包含 `invalid_mode: valid_only`。
7. 切换“全部”和“仅无效”，确认表格黄色行和无效原因展示正确。
8. 打开详情弹窗，确认字段完整。
9. 提交复核，确认提示成功。
10. 测试导出当前查询和导出选中记录。
11. admin 进入数据导入页，上传 Excel，确认导入结果和 warning 展示。
12. admin 进入审计日志页，确认日志列表可分页和查看详情。

## 7. 常见问题

### 7.1 打不开前端页面

先检查端口：

```powershell
netstat -ano | Select-String ':5173'
```

如果没有监听，重新启动：

```powershell
cd E:\vscode\excel查找\excel\frontend
npm.cmd run dev:mock
```

mock 预览优先访问：

```text
http://127.0.0.1:5173/
```

### 7.2 登录或查询接口失败

确认当前模式：

- `dev:mock`：不依赖后端，接口应返回 mock 数据。
- `dev`：依赖真实后端，必须确认后端 `http://localhost:8000/health` 可访问。

### 7.3 401 未授权

处理方式：

- 清理浏览器 localStorage。
- 重新登录。
- 确认后端默认账号是否仍为 `admin / admin123456`。

### 7.4 跨域问题

开发期推荐使用：

```env
VITE_API_BASE_URL=/api
VITE_BACKEND_TARGET=http://localhost:8000
```

前端请求 `/api`，由 Vite proxy 转发到后端，避免浏览器跨域。

### 7.5 日期筛选结果不可信

这是已知后端数据问题。后端 B 文档记录过 `bid_open_date` 导入异常，前端只负责展示和传参，不修正后端日期数据。

## 8. 前端验证命令

```powershell
cd E:\vscode\excel查找\excel\frontend
npm.cmd run test
npm.cmd run typecheck
npm.cmd run build
```

当前已验证：

```text
test       3 个测试文件，9 个测试通过
typecheck 通过
build     通过
```

## 9. 当前建议

前端日常开发优先使用 mock 模式：

```powershell
npm.cmd run dev:mock
```

等后端提供可用服务和测试数据后，再切换真实接口模式：

```powershell
npm.cmd run dev -- --host 127.0.0.1 --port 5173
```
